<?php  $month = ["Jan"]; 
//$output = "[";
 // debug(json_encode($views_graph, JSON_PRETTY_PRINT)); exit;
   //foreach ($views_graph as $graph){
       //array_push($month, date('M', strtotime($graph->txdate)));
             // $month .= '"'.date('M', strtotime($graph->txdate)).'"'.',';
        //  }
 // echo $output .= $month . "]";
 echo $month; 
  ?>
["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
